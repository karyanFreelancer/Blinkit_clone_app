import { isWeb } from "@tamagui/constants";
import * as React from "react";
var cachedResult = null;
function getReducedMotion() {
  var _window_matchMedia, _window_matchMedia1, _window;
  if (cachedResult !== null) return cachedResult;
  if (!isWeb) {
    cachedResult = false;
    return false;
  }
  if (typeof window === "undefined") {
    return false;
  }
  var _window_matchMedia_matches;
  cachedResult = (_window_matchMedia_matches = (_window_matchMedia1 = (_window = window).matchMedia) === null || _window_matchMedia1 === void 0 ? void 0 : (_window_matchMedia = _window_matchMedia1.call(_window, "(prefers-reduced-motion: reduce)")) === null || _window_matchMedia === void 0 ? void 0 : _window_matchMedia.matches) !== null && _window_matchMedia_matches !== void 0 ? _window_matchMedia_matches : false;
  return cachedResult;
}
function useReducedMotion(forceReducedMotion) {
  var [reducedMotion, setReducedMotion] = React.useState(function () {
    return forceReducedMotion !== null && forceReducedMotion !== void 0 ? forceReducedMotion : getReducedMotion();
  });
  React.useEffect(function () {
    var _window_matchMedia, _window, _mediaQuery_addEventListener;
    if (forceReducedMotion !== void 0) {
      setReducedMotion(forceReducedMotion);
      return;
    }
    if (!isWeb || typeof window === "undefined") return;
    var mediaQuery = (_window_matchMedia = (_window = window).matchMedia) === null || _window_matchMedia === void 0 ? void 0 : _window_matchMedia.call(_window, "(prefers-reduced-motion: reduce)");
    if (!mediaQuery) return;
    var handleChange = function (e) {
      cachedResult = e.matches;
      setReducedMotion(e.matches);
    };
    (_mediaQuery_addEventListener = mediaQuery.addEventListener) === null || _mediaQuery_addEventListener === void 0 ? void 0 : _mediaQuery_addEventListener.call(mediaQuery, "change", handleChange);
    return function () {
      var _mediaQuery_removeEventListener;
      (_mediaQuery_removeEventListener = mediaQuery.removeEventListener) === null || _mediaQuery_removeEventListener === void 0 ? void 0 : _mediaQuery_removeEventListener.call(mediaQuery, "change", handleChange);
    };
  }, [forceReducedMotion]);
  return reducedMotion;
}
export { useReducedMotion };
//# sourceMappingURL=useReducedMotion.native.js.map
