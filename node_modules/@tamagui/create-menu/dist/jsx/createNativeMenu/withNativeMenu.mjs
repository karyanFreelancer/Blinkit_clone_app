import { isWeb } from "@tamagui/web";
import { jsx } from "react/jsx-runtime";
function withNativeMenu({
  Component,
  NativeComponent
}) {
  if (isWeb) {
    return Component;
  }
  if (!NativeComponent) {
    return Component;
  }
  const Menu = props => {
    return /* @__PURE__ */jsx(NativeComponent, {
      ...props
    });
  };
  Menu.displayName = NativeComponent.displayName || Component?.displayName;
  return Menu;
}
export { withNativeMenu };
//# sourceMappingURL=withNativeMenu.mjs.map
